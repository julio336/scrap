<% form_for @scrapping do |f| %>
  <% if @scrapping.errors.any? %>
    <div id="error_explanation">	
      <h2><%= pluralize(@scrapping.errors.count, "error") %> prohibited this scrapping from being saved:</h2>

      <ul>
      <% @scrapping.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_area :url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
